# 🎨 Структура стилей после рефакторинга

## Файлы CSS

### 1. `/src/App.css` (5 строк)
```css
.app {
  display: flex;
  height: 100vh;
  background-color: #e9ecef;
}
```
**Назначение:** Только глобальный контейнер приложения

---

### 2. `/src/components/BlocksPanel/BlocksPanel.css` (145 строк)
**Содержит:**
- `.blocks-panel` - контейнер панели
- `.panel-tabs` - табы (Блоки/Костюмы)
- `.tab-button` - кнопки табов
- `.blocks-content`, `.costumes-content` - содержимое табов
- `.block-category` - категории блоков
- `.code-block` - стили блоков (motion, looks, control, events)
- `.costumes-grid` - сетка костюмов
- `.costume-item` - карточка костюма
- `.costume-emoji`, `.costume-name` - элементы костюма

**Цвета блоков:**
- `motion`: #4c9aff (синий)
- `looks`: #9966ff (фиолетовый)
- `control`: #ffab19 (оранжевый)
- `events`: #ffbf00 (желтый)

---

### 3. `/src/components/Workspace/Workspace.css` (288 строк)
**Содержит:**

#### Workspace
- `.workspace` - контейнер workspace
- `.workspace-header` - заголовок с кнопками
- `.workspace-content` - содержимое workspace

#### Code Area
- `.code-area` - зона для скрипта
- `.drop-zone` - зона сброса блоков
- `.dropped-block` - блок в скрипте
- `.dropped-block.executing` - подсветка выполняемого блока

#### Stage
- `.stage` - контейнер сцены
- `.canvas-container` - холст

#### Sprite
- `.sprite` - спрайт
- `.sprite.clickable` - кликабельный спрайт
- `.sprite.hidden` - скрытый спрайт
- `@keyframes pulse` - анимация пульсации

#### Speech Bubble
- `.speech-bubble` - речевой пузырь
- `.speech-bubble::after`, `.speech-bubble::before` - треугольник
- `@keyframes popIn` - анимация появления

#### Output Console
- `.output-console` - консоль
- `.output-line` - строка лога

---

## Импорты в компонентах

### BlocksPanel.tsx
```typescript
import './BlocksPanel.css';
```

### Workspace.tsx
```typescript
import './Workspace.css';
```

### App.tsx
```typescript
import './App.css';
```

---

## Цветовая схема

### Основные цвета
- Фон приложения: `#e9ecef`
- Фон панелей: `#f8f9fa`
- Белый: `#ffffff`
- Границы: `#dee2e6`

### Текст
- Основной: `#495057`
- Вторичный: `#6c757d`
- Темный: `#212529`

### Акценты
- Синий (активный): `#4c9aff`
- Зеленый (запуск): `#28a745`
- Красный (стоп): `#dc3545`
- Серый (сброс): `#6c757d`

### Блоки
- Motion: `#4c9aff`
- Looks: `#9966ff`
- Control: `#ffab19`
- Events: `#ffbf00`

---

## Анимации

### pulse (для кликабельного спрайта)
```css
@keyframes pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(76, 154, 255, 0.7); }
  50% { box-shadow: 0 0 0 10px rgba(76, 154, 255, 0); }
}
```

### popIn (для речевого пузыря)
```css
@keyframes popIn {
  0% { transform: translateX(-50%) scale(0); opacity: 0; }
  50% { transform: translateX(-50%) scale(1.1); }
  100% { transform: translateX(-50%) scale(1); opacity: 1; }
}
```

---

## Transitions

- Блоки: `transform 0.2s`
- Кнопки: `background-color 0.2s`, `all 0.2s`
- Спрайт: `transform 0.3s ease-out`
- Dropped Block: `all 0.3s ease`
- Костюмы: `all 0.2s`

---

## 🔧 Если стили не применяются

1. **Проверьте импорты** в компонентах
2. **Очистите кэш** браузера (Ctrl+Shift+R / Cmd+Shift+R)
3. **Перезапустите dev server:**
   ```bash
   npm run dev
   ```
4. **Проверьте консоль** браузера на ошибки CSS

---

## ✅ Все стили на месте!

- ✅ Панель блоков (левая сторона)
- ✅ Табы (Блоки/Костюмы)
- ✅ Цветные блоки (4 категории)
- ✅ Сетка костюмов
- ✅ Workspace (правая сторона)
- ✅ Кнопки управления
- ✅ Зона сброса блоков
- ✅ Сцена и спрайт
- ✅ Анимации (пульсация, речевой пузырь)
- ✅ Консоль вывода
