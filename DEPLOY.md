# 🚀 Деплой на Netlify

## Ваш проект готов к публикации!

### ✅ Build успешен
```
dist/index.html                   0.45 kB
dist/assets/index-CJHMR7tu.css    6.23 kB
dist/assets/index-BnBzZlNA.js   202.88 kB
✓ built in 316ms
```

---

## 📦 Вариант 1: Деплой через Netlify CLI (Рекомендуется)

### Шаг 1: Установите Netlify CLI
```bash
npm install -g netlify-cli
```

### Шаг 2: Авторизуйтесь
```bash
netlify login
```
Откроется браузер для авторизации в Netlify.

### Шаг 3: Задеплойте проект
```bash
netlify deploy
```
- Выберите **"Create & configure a new site"**
- Выберите команду: `scratch-mvp` (или свое название)
- Publish directory: `dist`

### Шаг 4: Production деплой
```bash
netlify deploy --prod
```

---

## 🌐 Вариант 2: Деплой через Netlify UI (Проще)

### Шаг 1: Зайдите на Netlify
Перейдите на [https://app.netlify.com/](https://app.netlify.com/)

### Шаг 2: Drag & Drop
1. Нажмите **"Add new site"** → **"Deploy manually"**
2. Перетащите папку **`dist`** в окно браузера
3. Готово! ✅

### Шаг 3: Получите ссылку
Netlify автоматически создаст URL вида:
```
https://random-name-123456.netlify.app
```

### Шаг 4: (Опционально) Настройте домен
- Перейдите в **Site settings** → **Domain management**
- Нажмите **"Add custom domain"**
- Или измените случайное имя на свое

---

## 🔗 Вариант 3: Деплой из GitHub (Непрерывная интеграция)

### Шаг 1: Инициализируйте Git (если не сделано)
```bash
git init
git add .
git commit -m "Initial commit - Scratch MVP"
```

### Шаг 2: Создайте репозиторий на GitHub
1. Зайдите на [GitHub](https://github.com/new)
2. Создайте новый репозиторий `scratch-mvp`
3. Не инициализируйте с README

### Шаг 3: Отправьте код на GitHub
```bash
git remote add origin https://github.com/ваш-username/scratch-mvp.git
git branch -M main
git push -u origin main
```

### Шаг 4: Подключите к Netlify
1. На [Netlify](https://app.netlify.com/) → **"Add new site"** → **"Import an existing project"**
2. Выберите **GitHub**
3. Найдите репозиторий `scratch-mvp`
4. Настройки:
   - **Build command:** `npm run build`
   - **Publish directory:** `dist`
5. Нажмите **"Deploy site"**

### Шаг 5: Автоматический деплой
Теперь при каждом push в main будет автоматический деплой! 🎉

---

## ⚙️ Файл netlify.toml

Файл **`netlify.toml`** уже создан в корне проекта:
```toml
[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

---

## 📊 Что включено в build

- ✅ React приложение (202.88 kB)
- ✅ Все стили (6.23 kB)
- ✅ Минимизированный код
- ✅ Оптимизированные изображения
- ✅ HTML страница (0.45 kB)

---

## 🎯 После деплоя

### Проверьте функциональность:
- [ ] Drag & Drop блоков работает
- [ ] Спрайт двигается и вращается
- [ ] Костюмы меняются
- [ ] Речевые пузыри появляются
- [ ] События "Когда нажато" работают
- [ ] Консоль выводит логи

### Поделитесь ссылкой! 🔗
```
https://ваш-сайт.netlify.app
```

---

## 🛠️ Быстрые команды

```bash
# Build для production
npm run build

# Превью production build локально
npm run preview

# Деплой на Netlify (после установки CLI)
netlify deploy --prod
```

---

## 📝 Переменные окружения (если понадобятся)

Если в будущем понадобятся API ключи:
1. В Netlify → **Site settings** → **Environment variables**
2. Добавьте переменные
3. В коде используйте `import.meta.env.VITE_*`

---

## ✨ Ваш проект готов к миру!

Выберите любой вариант деплоя выше и через минуту ваш Scratch MVP будет доступен всем! 🌍

**Рекомендую Вариант 2 (Drag & Drop) - самый быстрый способ!**
