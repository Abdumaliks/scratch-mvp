# 🎨 Scratch MVP - Визуальное программирование на React

[![React](https://img.shields.io/badge/React-18.2.0-61DAFB?logo=react&logoColor=white)](https://react.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.2.2-3178C6?logo=typescript&logoColor=white)](https://www.typescriptlang.org/)
[![Vite](https://img.shields.io/badge/Vite-7.3.1-646CFF?logo=vite&logoColor=white)](https://vitejs.dev/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

MVP сайта визуального программирования, похожего на [Scratch](https://scratch.mit.edu/), созданный с использованием React, TypeScript и Vite.

![Demo](https://via.placeholder.com/800x400/4c9aff/ffffff?text=Scratch+MVP+Demo)

## ✨ Особенности проекта

### 🏗️ Чистая архитектура
- **Context API** для управления состоянием
- **11 модульных компонентов** с разделением ответственности
- **Custom Hooks** (useDragAndDrop)
- **TypeScript** с полной типизацией
- **Модульные CSS** файлы

## 🎯 Возможности

- 🧩 **Drag & Drop блоки** - перетаскивайте блоки кода в рабочую область
- 🎭 **Интерактивная сцена** - спрайт с котом, который выполняет команды
- ▶️ **Выполнение скриптов** - запуск последовательности блоков
- 📤 **Консоль вывода** - отображение логов выполнения
- 🎨 **Категории блоков**:
  - **События** - триггеры для запуска программы
  - **Движение** - перемещение и поворот спрайта
  - **Внешность** - показать/скрыть спрайт, сообщения
  - **Управление** - задержки и циклы

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Откройте браузер по адресу [http://localhost:5173](http://localhost:5173)

### Сборка для продакшена

```bash
npm run build
```

### Предварительный просмотр продакшен-сборки

```bash
npm run preview
```

## 📖 Как использовать

1. **Перетащите блоки** из левой панели в область "Скрипт"
2. **Настройте значения** в блоках (шаги, градусы, секунды)
3. **Нажмите "Запустить"** для выполнения программы
4. **Наблюдайте** за спрайтом на сцене и логами в консоли
5. **Удаляйте блоки** кнопкой ✕ справа от каждого блока

## 🛠 Технологии

- **React 18** - библиотека для создания UI
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **CSS3** - стилизация компонентов

## 📝 Структура проекта

```
scratch/
## 📁 Структура проекта

```
src/
├── components/
│   ├── BlocksPanel/          # Панель с блоками кода
│   │   ├── BlocksPanel.tsx
│   │   ├── BlocksList.tsx
│   │   ├── CostumesList.tsx
│   │   └── BlocksPanel.css
│   ├── Workspace/            # Рабочая область
│   │   ├── Workspace.tsx
│   │   ├── WorkspaceHeader.tsx
│   │   ├── CodeArea.tsx
│   │   ├── Stage.tsx
│   │   ├── Sprite.tsx
│   │   ├── OutputConsole.tsx
│   │   └── Workspace.css
│   └── DroppedBlock/         # Блок в скрипте
│       └── DroppedBlock.tsx
├── context/
│   └── ScratchContext.tsx    # Context API + бизнес-логика
├── hooks/
│   └── useDragAndDrop.ts     # Логика Drag & Drop
├── types/
│   └── index.ts              # TypeScript типы
├── constants/
│   └── index.ts              # Константы (блоки, костюмы)
├── App.tsx                   # Главный компонент (17 строк!)
├── App.css                   # Минимальные глобальные стили
├── main.tsx                  # Точка входа
└── index.css                 # Глобальные стили
```

## 🏗️ Архитектура

### Context API
Всё состояние приложения хранится в `ScratchContext`:
- Список блоков в скрипте
- Состояние спрайта (позиция, поворот, костюм, видимость)
- Логи выполнения
- Текущий выполняемый блок
- Бизнес-логика (executeBlocks, executeBlock)

### Custom Hooks
- `useScratch()` - доступ к Context
- `useDragAndDrop()` - логика перетаскивания блоков

### Компоненты
11 небольших, переиспользуемых компонентов с чёткой ответственностью.

## 🎨 Типы блоков

### События (желтые)
- **When clicked** - начать выполнение при клике

### Движение (синие)
- **Move [N] steps** - двигаться на N шагов
- **Turn [N] degrees** - повернуться на N градусов  
- **Go to x: y:** - вернуться в центр

### Внешность (фиолетовые)
- **Say for 2 secs** - показать сообщение
- **Show** - показать спрайт
- **Hide** - скрыть спрайт

### Управление (оранжевые)
- **Wait [N] seconds** - подождать N секунд
- **Repeat [N]** - повторить N раз

## 🔮 Планы развития

- [ ] Добавить больше блоков (звуки, условия, переменные)
- [ ] Реализовать drag & drop для переупорядочивания блоков
- [ ] Добавить несколько спрайтов
- [ ] Сохранение/загрузка проектов
- [ ] Анимация поворота спрайта
- [ ] Редактор костюмов для спрайтов
- [ ] Блоки с условиями (if/else)
- [ ] Поддержка клавиатурных событий

## 📄 Лицензия

MIT
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```
