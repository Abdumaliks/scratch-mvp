# Scratch MVP - Визуальное программирование на React

MVP сайта визуального программирования, похожего на [Scratch](https://scratch.mit.edu/), созданный с использованием React, TypeScript и Vite.

## 🎯 Возможности

- 🧩 **Drag & Drop блоки** - перетаскивайте блоки кода в рабочую область
- 🎭 **Интерактивная сцена** - спрайт с котом, который выполняет команды
- ▶️ **Выполнение скриптов** - запуск последовательности блоков
- 📤 **Консоль вывода** - отображение логов выполнения
- 🎨 **Категории блоков**:
  - **События** - триггеры для запуска программы
  - **Движение** - перемещение и поворот спрайта
  - **Внешность** - показать/скрыть спрайт, сообщения
  - **Управление** - задержки и циклы

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Откройте браузер по адресу [http://localhost:5173](http://localhost:5173)

### Сборка для продакшена

```bash
npm run build
```

### Предварительный просмотр продакшен-сборки

```bash
npm run preview
```

## 🌐 Деплой на Netlify

### Способ 1: Автоматический скрипт (Рекомендуется)

```bash
./deploy.sh
```

Скрипт автоматически:
- Установит Netlify CLI (если нужно)
- Создаст production build
- Авторизует в Netlify
- Задеплоит проект

### Способ 2: Ручной деплой

```bash
# 1. Установите Netlify CLI
npm install -g netlify-cli

# 2. Авторизуйтесь
netlify login

# 3. Создайте build
npm run build

# 4. Деплой
netlify deploy --prod
```

### Способ 3: Drag & Drop (Самый простой)

1. Выполните `npm run build`
2. Зайдите на [netlify.com](https://app.netlify.com/)
3. Перетащите папку `dist` в браузер
4. Готово! ✅

📚 Подробная инструкция: [DEPLOY.md](./DEPLOY.md)

## 📖 Как использовать

1. **Перетащите блоки** из левой панели в область "Скрипт"
2. **Настройте значения** в блоках (шаги, градусы, секунды)
3. **Нажмите "Запустить"** для выполнения программы
4. **Наблюдайте** за спрайтом на сцене и логами в консоли
5. **Удаляйте блоки** кнопкой ✕ справа от каждого блока

## 🛠 Технологии

- **React 18** - библиотека для создания UI
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **CSS3** - стилизация компонентов

## 📝 Структура проекта

```
scratch/
├── src/
│   ├── App.tsx          # Главный компонент
│   ├── App.css          # Стили приложения
│   ├── main.tsx         # Точка входа
│   └── index.css        # Глобальные стили
├── public/              # Статические файлы
└── package.json         # Зависимости проекта
```

## 🎨 Типы блоков

### События (желтые)
- **When clicked** - начать выполнение при клике

### Движение (синие)
- **Move [N] steps** - двигаться на N шагов
- **Turn [N] degrees** - повернуться на N градусов  
- **Go to x: y:** - вернуться в центр

### Внешность (фиолетовые)
- **Say for 2 secs** - показать сообщение
- **Show** - показать спрайт
- **Hide** - скрыть спрайт

### Управление (оранжевые)
- **Wait [N] seconds** - подождать N секунд
- **Repeat [N]** - повторить N раз

## 🔮 Планы развития

- [ ] Добавить больше блоков (звуки, условия, переменные)
- [ ] Реализовать drag & drop для переупорядочивания блоков
- [ ] Добавить несколько спрайтов
- [ ] Сохранение/загрузка проектов
- [ ] Анимация поворота спрайта
- [ ] Редактор костюмов для спрайтов
- [ ] Блоки с условиями (if/else)
- [ ] Поддержка клавиатурных событий

## 📄 Лицензия

MIT
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```
