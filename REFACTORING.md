# Рефакторинг Scratch MVP

## 📁 Новая структура проекта

```
src/
├── components/
│   ├── BlocksPanel/
│   │   ├── BlocksPanel.tsx      - Панель с блоками и костюмами
│   │   ├── BlocksList.tsx       - Список доступных блоков
│   │   ├── CostumesList.tsx     - Список костюмов
│   │   └── index.ts
│   ├── Workspace/
│   │   ├── Workspace.tsx        - Основная рабочая область
│   │   ├── WorkspaceHeader.tsx  - Кнопки управления
│   │   ├── CodeArea.tsx         - Область с дропнутыми блоками
│   │   ├── Stage.tsx            - Сцена со спрайтом
│   │   ├── Sprite.tsx           - Компонент спрайта
│   │   └── index.ts
│   └── DroppedBlock/
│       └── DroppedBlock.tsx     - Отдельный блок в скрипте
├── context/
│   └── ScratchContext.tsx       - Context API для состояния
├── constants/
│   └── index.ts                 - Константы (блоки, костюмы)
├── types/
│   └── index.ts                 - TypeScript типы
├── hooks/
│   └── useDragAndDrop.ts        - Хук для drag & drop
├── App.tsx                      - Главный компонент
└── main.tsx                     - Точка входа

```

## 🔄 Что уже создано:

✅ `/src/types/index.ts` - Все TypeScript типы
✅ `/src/constants/index.ts` - Константы (блоки, костюмы, начальное состояние)
✅ `/src/context/ScratchContext.tsx` - Context с логикой выполнения блоков
✅ `/src/App.new.tsx` - Новая упрощенная версия App

## 📝 План по интеграции:

### Вариант 1: Постепенная миграция (Рекомендуется)
1. Оставить текущий `App.tsx` рабочим
2. Создать компоненты один за другим
3. Постепенно заменять части в App.tsx на новые компоненты
4. Тестировать после каждого изменения

### Вариант 2: Полная замена
1. Переименовать текущий `App.tsx` в `App.old.tsx`
2. Переименовать `App.new.tsx` в `App.tsx`
3. Создать все необходимые компоненты
4. Интегрировать логику

## 🎯 Преимущества нового подхода:

- ✅ **Разделение ответственности** - каждый компонент отвечает за свою часть
- ✅ **Переиспользуемость** - компоненты можно использовать повторно
- ✅ **Тестируемость** - легче писать тесты для маленьких компонентов
- ✅ **Читаемость** - код проще понять и поддерживать
- ✅ **Context API** - глобальное состояние без prop drilling
- ✅ **TypeScript** - строгая типизация во всех файлах

## 🚀 Следующие шаги:

Хотите продолжить рефакторинг? Я могу:
1. Создать все компоненты полностью
2. Показать как интегрировать их постепенно
3. Создать хуки для drag & drop логики
4. Перенести стили в модульные CSS файлы

Выберите подход и я продолжу! 🎨
